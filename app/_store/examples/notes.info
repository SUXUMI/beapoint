https://github.com/facebook/react-native/tree/master/packages/rn-tester/js/examples

Joi 
    // not works on react-native apps!!!
    https://github.com/sideway/joi/issues/2141
    npm i joi
    npm install text-encoding-polyfill
    issue with TextEncoder

Yup
    how to handle each field instead of all!
    https://dev.to/garryxiao/form-validation-with-yup-under-react-and-material-ui-4nhg
    NOT DOCUMENTED fetching error per key
    `
        Yup.reach(schemas, field).validate(value).then(result => {
            console.log(field, result)
        }).catch(result => {
            console.log(field, result)
        })
    `

React setState issue
    Accessing React State right after setting it
    https://dev.to/dance2die/accessing-react-state-right-after-setting-it-2kc8
    DEPREACATED - useState >> second param callback, need to use useEffect

setState
    is `async` but not returns the `promise` :(
    source: https://sentry.io/answers/forget-set-state-is-async/

Object.keys(..).forEach << !!!async

